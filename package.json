{
  "name": "financial-transaction-profiling-project",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts for Financial Transaction Profiling Showcase",
  "scripts": {
    "dev": "npm --prefix website run dev",
    "build": "npm --prefix website run build",
    "preview": "npm --prefix website run preview"
  }
}
