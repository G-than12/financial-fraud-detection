{
  "name": "financial-fraud-detection-project",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts for Financial Fraud Detection Showcase",
  "scripts": {
    "dev": "npm --prefix website run dev",
    "build": "npm --prefix website run build",
    "preview": "npm --prefix website run preview"
  }
}
